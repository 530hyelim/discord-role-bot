services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - PORT=3000
      - NODE_ENV=development
    volumes:
      - .:/opt/render/project/src
      - /opt/render/project/src/node_modules
      - /opt/render/project/src/vendor
